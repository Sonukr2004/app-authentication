import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: For Next.js 16, workspace root is auto-detected
  // Use 'npm run dev:clean' if you encounter build cache issues
};

export default nextConfig;
